-- Configuration
function love.conf(t)
	t.title = "XENON 4.0" -- The title of the window the game is in (string)
	t.window.width = 500        -- we want our game to be long and thin.
	t.window.height = 600

	-- For Windows debugging
	t.console = true
end
